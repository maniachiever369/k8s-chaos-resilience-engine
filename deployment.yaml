apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: payment-api
  name: payment-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: payment-api
  strategy: {}
  template:
    metadata:
      labels:
        app: payment-api
    spec:
      containers:
      - image: nginx:1.25
        name: nginx
        resources: {}
status: {}
